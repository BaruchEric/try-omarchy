# The source hibernates before a graphics driver binds.  The resumed branch
# removes the matching real-root file and probes virtio_gpu against the fresh
# target device, so no host VirGL context or virtqueue is persisted in swsusp.
blacklist virtio_gpu
