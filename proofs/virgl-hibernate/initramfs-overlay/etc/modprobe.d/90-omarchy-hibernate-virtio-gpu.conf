# The source hibernates before graphics or input drivers bind.  The resumed
# branch removes the matching real-root file and probes both drivers against
# the fresh target devices, so no host VirGL context or input virtqueue is
# persisted in swsusp.
blacklist virtio_gpu
blacklist virtio_input
