diff --git a/src/vrend_renderer.c b/src/vrend_renderer.c
index d94a9a1..df4409b 100644
--- a/src/vrend_renderer.c
+++ b/src/vrend_renderer.c
@@ -1143,6 +1143,13 @@ static void init_features(int gl_ver, int gles_ver)
          }
       }
    }
+
+#ifdef __EMSCRIPTEN__
+   /* WebGL 2 exposes renderbuffer multisampling, not GLES multisample
+    * textures. GLES 3.0 version detection would otherwise advertise a path
+    * that requires glTex*Multisample entry points unavailable in WebGL. */
+   clear_feature(feat_multisample);
+#endif
 }
 
 static void vrend_destroy_surface(struct vrend_surface *surf)
