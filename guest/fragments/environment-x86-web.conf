# The browser QEMU guest has no host 3D device, so make its portable CPU
# renderer explicit. The native ARM guest intentionally does not inherit these
# overrides and lets Mesa probe the Virtio GPU before falling back in-guest.
LIBGL_ALWAYS_SOFTWARE=true
GALLIUM_DRIVER=llvmpipe
