# QEMU-Wasm boots this initramfs on virtual hardware, not the builder's host.
MODULES=(virtio_pci virtio_blk virtio_gpu virtio_input usbhid hid_generic)
BINARIES=()
FILES=()
HOOKS=(base udev modconf kms keyboard keymap consolefont block filesystems fsck)
COMPRESSION="zstd"
