[Service]
# UWSM's upstream unit is Type=notify and normally allows 30 seconds for the
# compositor. The single-vCPU WebAssembly TCG guest can legitimately need far
# longer to page in Hyprland and llvmpipe, so retain a bounded web-only window.
TimeoutStartSec=15min
