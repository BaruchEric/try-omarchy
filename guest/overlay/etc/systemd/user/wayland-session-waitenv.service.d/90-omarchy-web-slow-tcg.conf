[Service]
# UWSM waits for the compositor to publish its session environment through
# this companion unit. Keep its bound equal to the compositor's web-only
# timeout so the helper cannot tear down a healthy but slow Hyprland startup.
TimeoutStartSec=15min
